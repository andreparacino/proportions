export enum VARIABLE_SYMBOL {
  X = "𝑥",
  Y = "𝑦",
  Z = "𝑧",
  W = "𝑤",
}

export enum MATH_SYMBOL {
  COLON = ":",
  EQUAL = "=",
}

export enum MEMBER {
  FIRST_MEMBER = "firstMember",
  SECOND_MEMBER = "secondMember",
  THIRD_MEMBER = "thirdMember",
  FOURTH_MEMBER = "fourthMember",
}

export enum ERROR {
  INVALID_FLOAT = 'To insert a floating number, use "." instead of ","',
  ONLY_NUMERIC = "Each input only accepts numeric values.",
}
